define([],function () {
	        return Backbone.Model.extend({
	            defaults: {
	            },
	            urlRoot: 'api/index/language',
	            initialize: function () {
	            }
	        });
		});